#!/bin/sh
# Run database migrations without Mix, using the compiled release.
# Invoked by the Helm migration Job (pre-install/pre-upgrade hook).
set -eu
cd -P -- "$(dirname -- "$0")/.."
exec ./bin/demo eval "Demo.Release.migrate()"
